export function getOtpSupportHint() {
  return "Use the latest OTP from the configured verification channel for this environment."
}

export function getOtpDeliveryHint() {
  return "We will send a fresh OTP using the verification delivery configured for this environment."
}
